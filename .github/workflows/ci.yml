name: CI/CD

on:
  push:
    branches: [main, master]
    paths-ignore:
      - README.md
  pull_request:
    branches: [main, master]
    paths-ignore:
      - README.md

jobs:
  build:
    name: Build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSBuild.exe
        uses: microsoft/setup-msbuild@v2

      - name: Restore dependencies
        run: |
          msbuild 1.5\Source\CaravanMechRepairs.sln /t:Restore /p:Configuration=Release
          msbuild 1.6\Source\CaravanMechRepairs.sln /t:Restore /p:Configuration=Release

      - name: Build assemblies
        run: |
          msbuild 1.5\Source\CaravanMechRepairs.sln /p:Configuration=Release
          msbuild 1.6\Source\CaravanMechRepairs.sln /p:Configuration=Release

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CaravanMechRepairs
          path: |
            1.5\Assemblies
            1.6\Assemblies
            About\About.xml
            About\Preview.png
            Textures\**\*.png